DJANGO DRF + SIMPLE JWT + REDIS

Django приложение с поддержкой API для авторизации и получение профиля аккаунта.

Используемые технологии:
  + Django
  + REST FRAMEWORK
  + SIMPLE JWT 
  + POSTGRESS
  + REDIS
  + NGINX
  - Web-Socket (Пока не сделанно)
